using UnityEngine;
using System.Collections;

public class CollisionMiss : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Physics.IgnoreCollision(this.transform.gameObject.collider, collider);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
